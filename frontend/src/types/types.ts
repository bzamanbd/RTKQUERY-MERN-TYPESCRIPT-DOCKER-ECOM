export type User = {
    name?: string;
    email: string;
    phone?: string;
    address?: string;
    avatar?: string;
    question?: string;
    role?: string;
    isBanned?: boolean;
    orders?: []
}
